## Tasks

- [x] Hide desktop table Stock Físico `<th>` from consumers: add `v-if="!isConsumidorRole"` to line ~697
- [x] Hide desktop table Stock Físico `<td>` from consumers: add `v-if="!isConsumidorRole"` to line ~877
- [x] Hide mobile grid Stock Físico block from consumers: add `v-if="!isConsumidorRole"` to line ~1046
- [x] Verify no other references to Stock Físico in Show.vue remain unhandled
